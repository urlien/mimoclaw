# 杰西卡文件导入问题记录

## 时间：2026-08-01 23:50 ~ 2026-08-02 03:30

## 一、问题现象

赵在手机上使用杰西卡，点击"从云端恢复"后：
- 页面显示"✅ 恢复成功，3秒后刷新..."
- 刷新完成后所有数据为空
- 聊天记录、日记、设置、头像全部丢失

控制台显示：
```
[CloudSync] 备份版本：v1，当前版本：v4
[CloudSync] 补全了 23 个缺失字段
[CloudSync] 正在从 v1 备份恢复（已补全 23 个字段）...
[CloudSync] 已恢复 33 条数据
```

关键数据：
- 备份版本：v1（应为 v4）
- 恢复数据量：33 条（应为 1426 条记忆 + 1423 条聊天记录 + 65 篇日记）
- 补全字段：23 个（说明备份格式与当前代码不匹配）

## 二、根因分析

### 根因1：浏览器缓存（最主要的原因）

**问题：** 浏览器缓存了 GitHub API 的响应。赵之前测试时下载的是 v1 格式的旧备份，浏览器将其缓存。之后每次点"从云端恢复"，浏览器都返回缓存的旧响应（v1），而不是从 GitHub 下载最新的 v4 格式备份。

**验证：** 在 F12 → Network → 勾选"Disable cache"后，恢复成功，显示"备份版本：v4"，恢复了完整数据。

**修复：** 在 `_getFile()` 和大文件下载的 `fetch` 请求里加 `cache: 'no-store'`。

```javascript
// 修改前
const res = await fetch(apiUrl, {
  headers: { 'Authorization': `token ${token}`, 'Accept': '...' },
});

// 修改后
const res = await fetch(apiUrl, {
  cache: 'no-store',
  headers: { 'Authorization': `token ${token}`, 'Accept': '...' },
});
```

### 根因2：IndexedDB autoIncrement冲突（Claude发现）

**问题：** `_restoreData()` 函数在写入 IndexedDB 时，使用 `store.add()` 方法。如果备份数据包含 `id` 字段，且该 `id` 与 IndexedDB 的 autoIncrement 计数器冲突，`add()` 会静默失败。

**具体表现：**
- `dbAddMemory()` 使用 `store.add(m)`，如果 `m` 已有 `id` 字段且该 `id` 已存在，`add()` 抛出 ConstraintError
- 错误被 `catch(e) {}` 吞掉，没有任何报错
- 1426 次 `add()` 调用，每次都打开新的数据库连接，效率极低

**修复：** Claude创建 `_idbBulkRestore()` 函数，把 clear + add 全部放进同一个事务，await `tx.oncomplete` 确保真正落盘。

### 根因3：备份格式版本不匹配

**问题：** `_restoreData()` 检测到备份版本（v1）低于当前版本（v4），触发"低版本兼容"逻辑，用 DEFAULTS 对象补全缺失字段。但 v1 备份的数据结构完全不同——设置存在 `jessica_settings` dict 里，而不是独立的 `jessica_apiBase`、`jessica_apiKey` 等 key。

**修复：** 在 `_restoreData()` 的 localStorage 写入循环之前，加兼容逻辑，把 `jessica_settings` dict 拆分为独立 key。

### 根因4：file:// 协议 CORS 限制

**问题：** 从本地双击打开 HTML 文件（`file:///D:/下载/jessica_full.html`），浏览器会阻止 `fetch` 请求外部 API（包括 GitHub API），导致 `_getFile()` 直接失败。

**解决方案：** 部署到 GitHub Pages，通过 HTTPS 访问。

## 三、修复清单

| # | 修复 | 状态 | 说明 |
|---|---|---|---|
| 1 | `cache: 'no-store'` | ✅ | 防止浏览器缓存 GitHub API 响应 |
| 2 | `_idbBulkRestore()` | ✅ | 单事务原子写，避免 autoIncrement 冲突 |
| 3 | `jessica_settings` 兼容 | ✅ | 旧版备份设置 dict 拆分为独立 key |
| 4 | `loadHistory()` 读取顺序 | ✅ | 先读 IndexedDB，再读 localStorage |
| 5 | GitHub Pages 部署 | ✅ | `https://urlien.github.io/jessica-diary/jessica_full.html` |
| 6 | `jessica-data` 改公开 | ✅ | 启用 GitHub Pages 所需 |

## 四、教训

1. **浏览器缓存是隐形杀手** — GitHub API 的响应会被浏览器缓存，必须加 `cache: 'no-store'`
2. **IndexedDB 的 autoIncrement 不会重置** — `clear()` 清空数据但不重置计数器，备份数据的 `id` 字段会导致冲突
3. **catch(e) {} 会吞掉所有错误** — 写入失败但函数仍返回 `true`，页面显示"恢复成功"但实际什么都没恢复
4. **备份版本兼容需要向前兼容** — 恢复代码需要能处理所有历史版本的格式
5. **file:// 协议受 CORS 限制** — 必须通过 HTTP 服务器访问

## 五、遗留问题

1. **resumePendingReply** — 导入数据后页面 reload 仍会触发 API 调用（已讨论但未修改）
2. **打包 APK** — 编译环境需要下载 611MB（jessica-build-env），预估 15-25 分钟

---

**记录人：** 凯西
**记录时间：** 2026-08-02 03:45 GMT+8
